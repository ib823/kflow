# KerjaFlow Environment Configuration Example
# ==============================================================================
#
# ZERO COST CONFIGURATION
# All services are self-hosted or free tier only.
# No cloud service charges possible.
#
# Copy this file to .env and customize for your environment.
#
# ==============================================================================

# ===========================================
# DATABASE CONFIGURATION (Self-hosted PostgreSQL)
# ===========================================
POSTGRES_DB=kerjaflow
POSTGRES_USER=odoo
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# ===========================================
# REDIS CONFIGURATION (Self-hosted Redis)
# ===========================================
REDIS_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# ===========================================
# JWT AUTHENTICATION (Self-hosted, no external service)
# ===========================================
# CRITICAL: Generate a strong secret with: openssl rand -hex 64
JWT_SECRET_KEY=GENERATE_WITH_OPENSSL_RAND_HEX_64

# Token lifetimes
ACCESS_TOKEN_EXPIRE_MINUTES=1440
REFRESH_TOKEN_EXPIRE_DAYS=7
PIN_VERIFICATION_EXPIRE_MINUTES=5

# ===========================================
# APPLICATION SETTINGS
# ===========================================
APP_ENV=production
DEBUG=false

# API Base URL (your self-hosted Odoo server)
API_BASE_URL=https://your-domain.com/api/v1

# Mobile App Settings
MOBILE_MIN_VERSION=1.0.0
FORCE_UPDATE=false

# ===========================================
# LOGGING (Console/file only - no external services)
# ===========================================
LOG_LEVEL=INFO
