# KerjaFlow Environment Configuration Example
# ==============================================================================
#
# ZERO COST CONFIGURATION
# All services are self-hosted or free tier only.
# No cloud service charges possible.
#
# Copy this file to .env and customize for your environment.
#
# ==============================================================================

# ===========================================
# DATABASE CONFIGURATION (Self-hosted PostgreSQL)
# ===========================================
POSTGRES_DB=kerjaflow
POSTGRES_USER=odoo
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# ===========================================
# REDIS CONFIGURATION (Self-hosted Redis)
# ===========================================
REDIS_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# ===========================================
# JWT AUTHENTICATION - RS256 (Asymmetric Keys)
# ===========================================
# SECURITY: Uses RS256 algorithm with RSA keypair.
# - Private key: Used for signing tokens (KEEP SECRET!)
# - Public key: Used for verifying tokens (can be shared)
#
# Generate keys with: infrastructure/scripts/generate_jwt_keys.sh
# This will create private.pem, public.pem, and jwt_keys.env
#
# For production, store keys in a secrets manager!

# RSA Private Key (for signing - KEEP SECRET!)
# Paste the entire key including BEGIN/END markers
JWT_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----
PASTE_YOUR_PRIVATE_KEY_HERE
-----END RSA PRIVATE KEY-----"

# RSA Public Key (for verification)
JWT_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----
PASTE_YOUR_PUBLIC_KEY_HERE
-----END PUBLIC KEY-----"

# JWT Algorithm (DO NOT CHANGE - must be RS256 for security)
JWT_ALGORITHM=RS256

# Token lifetimes
JWT_ACCESS_EXPIRE_HOURS=24
JWT_REFRESH_EXPIRE_DAYS=7
JWT_PIN_VERIFICATION_EXPIRE_MINUTES=5

# ===========================================
# APPLICATION SETTINGS
# ===========================================
APP_ENV=production
DEBUG=false

# API Base URL (your self-hosted Odoo server)
API_BASE_URL=https://your-domain.com/api/v1

# Mobile App Settings
MOBILE_MIN_VERSION=1.0.0
FORCE_UPDATE=false

# ===========================================
# LOGGING (Console/file only - no external services)
# ===========================================
LOG_LEVEL=INFO
