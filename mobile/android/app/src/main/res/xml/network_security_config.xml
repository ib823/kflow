<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Production Configuration -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Development/Debug Configuration -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>

    <!-- Allow localhost for development -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
    </domain-config>

    <!-- Pin certificates for production API -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">api.kerjaflow.my</domain>
        <pin-set expiration="2026-01-01">
            <!-- Add actual certificate pins here -->
            <pin digest="SHA-256">PLACEHOLDER_PIN_1</pin>
            <pin digest="SHA-256">PLACEHOLDER_PIN_2</pin>
        </pin-set>
    </domain-config>
</network-security-config>
